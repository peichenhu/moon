{
    "name": "mono",
    "private": true,
    "license": "MIT",
    "version": "0.0.0",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "@changesets/cli": "^2.27.1"
    }
}
